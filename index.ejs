<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>memories</title>
    <!-- <link rel="stylesheet" href="./style.css"> -->
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk&display=swap');

        :root {
            --background: #161311;
            --primary: #dde1e7;;
            --secondary: #191410;
            --text: #617e94;
            --accent: hsl(206, 21%, 76%);

            --universal-roundness: 18px;
            --univeral-font: 'Space Grotesk', sans-serif;
            --universal-gap: 14px;
        }

        * {
            font-family: var(--univeral-font);
            font-size: 18px;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            width: 200%;
            height: 100vh;
            background-color: #dde1e7;

            margin: 0 auto;
            padding: 0;
            display: flex;

            overflow-y: hidden;
            overflow-x: hidden;
        }

        .section {
            width: 100%;
            background-color: var(--primary);
            height: 100vh auto;
        }

        #stats {
            display: grid;
            grid-template-columns: 200px 400px;
            grid-template-areas: 
            "a b"
            "c d";
            grid-template-rows: 200px 200px;
            gap: var(--universal-gap);
            place-content: center;
        }

        @media only screen and (max-width: 700px) {
            #stats {
                grid-template-areas: 
                "a c"
                "b b"
                "d d";
                grid-template-columns: 150px 150px;
                grid-template-rows: 150px 150px 150px;
                /* transform: translate(0, 50px); */
            }
        }

        .tile {
            border-radius: var(--universal-roundness);
        }

        .grad {
            background: rgb(189,138,219);
            /* opacity: 20%; */
            background: linear-gradient(180deg, rgb(164, 113, 194) 0%, rgba(216,122,122,1) 100%);
        }

        .sl {
            display: flex;
            flex-direction: row;
            gap: 23px;
        }

        .slider {
            height: 100%;
            width: 50px;
            border-radius: 78px;
            background-color: #9DBFF1;
            position: relative;
        }

        #proceed {
            height: 100%;
            border-radius: var(--universal-roundness);
            background-color: #8792F5;
            text-decoration:none;
            width: 127px;
            display: grid; place-content: center;
        }

        #proceed > h1 {
            color: white; font-size: xx-large;
            text-decoration:none;
        }

        #proceed {
            transition: linear all 200ms;
        }

        .litlun {
            background-color: #FFFFFF;
            width: 100%;
            height: 76%;
            border-radius: 78px;
            position: absolute;
            bottom: 0;
        }

        #proceed:hover {
            background-color: #c568f0;
            outline: #000000 1px solid;
        }

        #proceed:hover > h1 {
            color: black;
            font-size: 32px;
        }
        #content > * {
            color: #191410;
            text-align: center;
        }

        #content {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: var(--universal-gap);
        }

        #content > h1 {
            font-size: xx-large;
        }
    </style>
</head>
<body>

    <div class="section" id="stats">
        <div  style="font-size: larger; color: var(--primary);background-color: #DC9999; grid-area: a; display: grid; place-content: center;" class="tile">Today is <br> <%=locals.date %> </div>
        <div style="font-size: larger; color: #0f1c94; opacity: 100%; display: grid; place-content: center; background-color: #61E8D8; grid-area: b;" class="tile">Hair: <%=locals.outfit.hair  %> <br> Shirt: <%=locals.outfit.shirt  %> <br> Leggings: <%=locals.outfit.leggings  %></div>
        <div class="tile grad" style="font-size: larger; color: #dde1e7; opacity: 100%; display: grid; place-content: center; grid-column: span 1; grid-area: c;">Temp: <%=locals.weather.temperature%> <br> <%=locals.weather.humidity%>% Humid <br> <%=locals.weather.comment%> <br> <%=locals.weather.location%></div>
        <div class="tile sl" style="grid-area: d;">
            <div  class="slider"><div style="height: <%=locals.mood.anger%>%; display: grid; place-items: center" class="litlun">💢</div></div>
            <div class="slider"><div style="height: <%=locals.mood.happiness%>%; display: grid; place-items: center" class="litlun">😊</div></div>
            <div class="slider"><div style="height: <%=locals.mood.sadness%>%; display: grid; place-items: center" class="litlun">😭</div></div>
            <div class="slider"><div style="height: <%=locals.mood.charge%>%; display: grid; place-items: center" class="litlun">⚡</div></div>
            <a id="proceed" href="#content"><h1>></h1></a>
        </div>
    </div>
    <div style="background-color: #dde1e7" id="content" class="section">
        <h1><%=locals.title  %></h1>
        <h2 style="padding: 10px;"><%=locals.text  %></h2>
    </div>

</body>
</html>